[tool.isort]
profile = "google"
line_length = 80
multi_line_output = 3

[tool.pylint.messages_control]
disable = [
    "C0330", # Handled by yapf
    "C0326", # Handled by yapf
]

[tool.pylint.format]
max-line-length = 80
